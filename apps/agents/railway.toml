[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm --filter shared build && pnpm --filter agents build"

[deploy]
startCommand = "node apps/agents/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
