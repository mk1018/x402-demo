[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm --filter shared build && pnpm --filter seller build"

[deploy]
startCommand = "node apps/seller/dist/index.js"
restartPolicyType = "ON_FAILURE"
